name: Exemplo de Fluxo de Trabalho

on:
  workflow_dispatch:

jobs:
  acao_inicial_job:
    runs-on: ubuntu-latest
    steps:
      - name: Implime OLA
        run: echo "OLÁ"
      - name: Imprime TCHAU
        run: echo "TCHAU"
